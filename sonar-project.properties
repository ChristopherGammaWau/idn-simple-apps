# must be unique in a given SonarQube instance
sonar.projectKey=simpleapps

# --- optional properties ---

# defaults to project key
sonar.projectName=Simple Apps
# defaults to 'not provided'
sonar.projectVersion=1.0
 
# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=.
 
# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

# Exclude directories from scanning
sonar.exclusions=app/coverage/**, app/node_modules/**, app/testing/**, app/Dockerfile, docker-compose.yaml

# Add testing files
sonar.tests=app/testing/

# Coverage report (depends on the tools used for testing`)
sonar.javascript.lcov.reportPaths=coverage/lcov.info

